<!-- Start Template class-names.md.ejs -->
<? function link(name) { ?>[<?= name ?>](#<?= name ?>)<? } ?>

# Class Names

## Table of Contents

<? docfiles.forEach(function(doc) { ?>
  <? doc.javadoc.forEach(function(group) { ?><? if (group.ignore) { return } ?>
    * <? link(group.name) ?><? group.children.forEach(function(className) { ?><?
      if (className.ignore) { return } ?>
      ** <? link(className.value) ?><?
    }) ?><?
  }) ?><?
}) ?>

<? docfiles.forEach(function(doc) { ?>
  <!-- Start <?= doc.filename ?> -->

  <? doc.javadoc.forEach(function(group) { ?>
    <? if (group.ignore) { return } ?>
    ## <?= group.name ?>

    <?= group.description.full ?>

    ### Summary

    Name | Description | Usage | Client HTML
    ---- | ----------- | ----- | -----------
    <? group.children.forEach(function(row) { ?><? if (row.ignore) { return } ?><?
      link(row.value) ?> | <?= row.description.summary ?> | <?= row.usage ?> | <?= row.clientHTML ?>
    <? }) ?>

    <? if (group.see) { ?>
      See: <?= group.see ?>
    <? } ?>

    ### Details

    <? group.children.forEach(function(className) { ?>
      <? if (className.ignore) { return } ?>
      #### <?= className.value ?>

      <?= className.description.full ?>

      <? if (className.parentElement) { ?>
        Parent Element: <? link(className.parentElement.value) ?>
      <? } ?>

      <? if (className.see) { ?>
        See: <?= className.see ?>
      <? } ?>
    <? }) ?>
  <? }) ?>

  <!-- End <?= doc.filename ?> -->
<? }) ?>
<!-- End Template class-names.md.ejs -->

